0, 'path' => '/', 'domain' => '', 'secure' => $isHttps, 'httponly' => true, 'samesite' => 'Lax', ]); } // ------------------------------------------------------------ // FILE UPLOADS // ------------------------------------------------------------ /** * Maximum allowed application upload size: * 2 MB. */ define( 'MAX_UPLOAD_SIZE', 2 * 1024 * 1024 ); /** * Allowed image MIME types for branding uploads. */ define( 'ALLOWED_IMAGE_MIME_TYPES', [ 'image/jpeg', 'image/png', 'image/webp', 'image/gif', ] ); // ------------------------------------------------------------ // BRAND DEFAULTS // ------------------------------------------------------------ define( 'DEFAULT_PRIMARY_COLOR', '#0B2C5F' ); define( 'DEFAULT_SECONDARY_COLOR', '#1D4ED8' ); define( 'DEFAULT_BACKGROUND_COLOR', '#F7F9FC' ); // ------------------------------------------------------------ // SECURITY // ------------------------------------------------------------ /** * CSRF token lifetime: * 2 hours. */ define( 'CSRF_TOKEN_LIFETIME', 7200 ); /** * Login security. */ define( 'MAX_LOGIN_ATTEMPTS', 5 ); /** * Login-attempt checking window: * 15 minutes. */ define( 'LOGIN_ATTEMPT_WINDOW', 900 ); // ------------------------------------------------------------ // ERROR HANDLING // ------------------------------------------------------------ if (APP_ENV === 'development') { error_reporting(E_ALL); ini_set( 'display_errors', '1' ); ini_set( 'display_startup_errors', '1' ); } else { error_reporting(E_ALL); /** * Never expose PHP/database details to production visitors. */ ini_set( 'display_errors', '0' ); ini_set( 'display_startup_errors', '0' ); ini_set( 'log_errors', '1' ); } // ------------------------------------------------------------ // CREATE UPLOAD DIRECTORY IF NEEDED // ------------------------------------------------------------ if ( !is_dir(UPLOAD_PATH) && !mkdir(UPLOAD_PATH, 0755, true) && !is_dir(UPLOAD_PATH) ) { error_log( 'Notify Tools: Unable to create uploads directory.' ); }
